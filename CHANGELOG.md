# Changelog

All notable changes to this project will be documented in this file.

## [1.3] - 2026-04-29
### Added
- 在设置页面（Settings -> Other Settings -> File Template）和创建对话框中显示模板序号列（“#”），序号从 1 开始显示，便于用户识别模板顺序。
- 模板编辑器保留原始多行格式（换行/空白），新建时会保留复制的内容格式。
- 新增导入/导出（JSON）按钮放置于提示区末端，导入为追加模式并使用 Jackson 标准库处理特殊字符。
- 右键菜单中的“File Template”项现在使用 16x16 图标，并修复了图标缩放方法（已移除 scaleIconToSize）。
- 创建文件后自动在编辑器中打开新文件。

### Changed
- 优化 Settings 页面列宽与编辑器布局：模板名称/类型列宽调整为 80，模板内容列调整为 200，字体增大；模板内容编辑器宽度缩小以避免遮挡启用开关。
- 表格操作按钮左对齐，导入/导出按钮移至提示区；提示区英文文案改为中文。
- 创建对话框中若模板列表非空，默认选中第一个模板以减少用户点击。

### Fixed
- 修复 NewVueTemplateAction 在非 EDT 线程访问 EDT-only 数据导致的异常问题（遵循 ActionUpdateThread 规范）。
- 修复导入取消时误报导入成功的问题。
- 修复导出时特殊字符导致 JSON 格式错误的问题（切换为 Jackson）。
- 修复右键菜单图标尺寸过大的问题并设置为 16x16。

### Notes
- 插件 ID 自 早期版本的历史发布中被调整以匹配 Marketplace 条目（请保持插件 ID 不要在 Marketplace 发布后更改）。

## [1.2] - 2026-04-29
### Added
- 调整图标，提供多尺寸 SVG 资源（16/32/40/80/128），并修复右键菜单图标尺寸与显示问题。
- 在设置页面与创建对话框中为模板显示序号（从 1 开始），并在创建对话框中默认选中第一个模板（如存在）。

### Changed
- 将版本号更新为 1.2，并在构建配置中更新发布说明。

### Fixed
- 修复导入/导出 JSON 的序列化/反序列化问题，替换为 Jackson 标准库以支持特殊字符。
- 修复了一些 UI 文本与布局问题，使设置页更易用。

## [1.1] - 2026-04-24
### Added
- 在设置页面（Settings -> Other Settings -> File Template）中新增了模板序号列（“#”），从 1 开始显示，便于用户识别模板顺序。
- 在“创建文件”对话框中为模板列表项增加了序号前缀（从 1 开始），并默认选中第一个模板（如果存在）。
- 优化了配置页面的列宽与字体，模板名称/类型列长度调整为 80，模板内容列长度调整为 200。
- 导入/导出功能改进：使用标准 JSON 序列化库（Jackson），导入为追加模式，导出为标准 JSON 文件，修复了特殊字符导致的解析错误。

### Changed
- 修正 `plugin.xml` 中的插件 id（由 `com.cuiwang.FileTemplate-Plugin` 更名为 `com.cuiwang.filetemplate`）以满足 Marketplace 校验。
- 新增设置页面序号更新逻辑，保持序号与列表项一致。
- 将 `NewVueTemplateAction` 的模板列表从字符串变为对象列表，渲染器显示序号和模板名称。

### Fixed
- 修复了多行模板内容编辑器在保存时丢失格式（现在保留原有换行与空白）。
- 修复了导入取消时误报导入成功的问题。
- 修复了部分 AndAction 更新线程使用问题，并规范了 action 的初始化。

### Notes
- 若需序号随表格排序动态变化（即点击列头排序后序号自动更新为可视顺序），可在后续版本实现；当前实现序号代表持久化顺序（配置列表中的顺序）。
